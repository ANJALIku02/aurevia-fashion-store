'use client';import{useMemo,useState}from'react';import ProductCard from'../components/ProductCard';import{products}from'../data/products';export default function ShopClient(){const[q,setQ]=useState('');const[cat,setCat]=useState('All');const[sort,setSort]=useState('featured');const shown=useMemo(()=>{let a=products.filter(p=>(cat==='All'||p.category===cat)&&p.name.toLowerCase().includes(q.toLowerCase()));return[...a].sort((x,y)=>sort==='low'?x.price-y.price:sort==='high'?y.price-x.price:0)},[q,cat,sort]);return <><div className="tools"><input aria-label="Search products" placeholder="Search products…" value={q} onChange={e=>setQ(e.target.value)}/><div>{['All','Women','Men','Unisex'].map(x=><button className={cat===x?'active':''} onClick={()=>setCat(x)} key={x}>{x}</button>)}</div><select value={sort} onChange={e=>setSort(e.target.value)}><option value="featured">Featured</option><option value="low">Price: low to high</option><option value="high">Price: high to low</option></select></div><p className="results">{shown.length} pieces</p><div className="product-grid">{shown.map(p=><ProductCard p={p} key={p.slug}/>)}</div>{!shown.length&&<p className="empty">No products match your search.</p>}</>}

