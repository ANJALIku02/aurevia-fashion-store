'use client';import Link from'next/link';import{Product,money}from'../data/products';import{useCart}from'./CartProvider';export default function ProductCard({p}:{p:Product}){const{saved,toggleSaved}=useCart();return <article className="product-card"><div className="product-image">{p.tag&&<span className="tag">{p.tag}</span>}<button className={`save ${saved.includes(p.slug)?'on':''}`} onClick={()=>toggleSaved(p.slug)} aria-label={`Save ${p.name}`}>♡</button><Link href={`/product/${p.slug}`}><img src={p.image} alt={p.name}/><span className="quick">View details ↗</span></Link></div><small>{p.category}</small><h3><Link href={`/product/${p.slug}`}>{p.name}</Link></h3><p>{money(p.price)} {p.oldPrice&&<del>{money(p.oldPrice)}</del>}</p></article>}

