import{notFound}from'next/navigation';import type{Metadata}from'next';import{products,getProduct}from'../../data/products';import ProductPurchase from'./ProductPurchase';export function generateStaticParams(){return products.map(p=>({slug:p.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const p=getProduct((await params).slug);if(!p)return{};return{title:`${p.name} | AUREVIA`,description:p.description,openGraph:{images:[p.image]}}}export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const p=getProduct((await params).slug);if(!p)notFound();return <main className="section page"><ProductPurchase p={p}/></main>}

